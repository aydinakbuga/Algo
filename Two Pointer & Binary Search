"""
A = list
key = Target Value
sorted array for Binary Search
O(logN) time comp.
0(3) space comp.
"""

def binSearch(A,left,right,key):
    while left <= right:
        mid = (left+right) // 2
        if A[mid] == key:
            return mid
        elif A[mid] < key:
            left = mid+1
        else:
            right = mid-1
        print(mid)
    return -1
